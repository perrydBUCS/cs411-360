/* Log user in from landing page. In this iteration the password is
just the user's BU ID, which has been stored in the database. As a
minor nod to security, th ID is at least hashed, and that's what gets
checked. We'll use a JWT on the cookie to validate each request.
 */

